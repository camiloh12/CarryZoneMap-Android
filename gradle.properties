org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Java 21 is required for this project
# Gradle will use Java from JAVA_HOME or the IDE's configured JDK
# On CI, GitHub Actions sets up Java 21 automatically
# For local development, ensure Android Studio/IntelliJ is configured to use Java 21
# If needed, you can override locally by uncommenting and setting the path:
# org.gradle.java.home=/usr/lib/jvm/java-1.21.0-openjdk-amd64